name: translate

on:
    schedule:
        -   cron: 30 2 1 * *
    workflow_dispatch:

permissions: write-all

jobs:
    translate:
        uses: Laravel-Lang/.github/.github/workflows/translate.yml@main
